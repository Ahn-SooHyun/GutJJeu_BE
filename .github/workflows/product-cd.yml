name: Product Service CD
on:
  release:
    types: [published]
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
      - name: Inject application.yml
        run: |
          mkdir -p ./Product/src/main/resources
          cat << 'EOF' > ./Product/src/main/resources/application.yml
          ${{ secrets.YML_PRODUCT }}
          EOF
      - name: Grant execute permission for gradlew
        run: chmod +x ./Product/gradlew
      - name: Build with Gradle
        run: cd Product && ./gradlew clean build -x test
      - name: Log in to Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./Product
          push: true
          tags: ghcr.io/ahn-soohyun/gutjjeu-product:${{ github.event.release.tag_name }}
